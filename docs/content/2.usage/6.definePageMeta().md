---
title: definePageMeta()
description: Use typed definePageMeta in your pages
navigation:
  icon: i-lucide-file-code
---

# definePageMeta

You will have autocompletion and typecheck for multiple properties in `definePageMeta`, both as a global import or from the `@typed-router` alias.

Like `useRoute`, it has a strict mode where you can assert the current page.

Properties enabled:

- `validate`
- `redirect`
- `key`

::warning
Due to TypeScript limitations, return type for `redirect` may be buggy or not display anything.
You can use the `helpers` util as a workaround. See [Helpers doc](/usage/helpers).
::

Example:

```ts
definePageMeta({
  validate(route) { // <- Typed
    return true
  }
})
```
